

import 'package:flutter/material.dart';

class ChallangePage extends StatelessWidget {
  const ChallangePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Challange Page")),
    );
  }
}